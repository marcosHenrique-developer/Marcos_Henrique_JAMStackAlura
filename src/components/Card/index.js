/* eslint-disable arrow-body-style */
import React from 'react';
import { MenuWrapper } from '../WrapperProjetos';

// eslint-disable-next-line import/prefer-default-export
export const Card = () => {
  // eslint-disable-next-line react/jsx-filename-extension
  return <MenuWrapper />;
};
